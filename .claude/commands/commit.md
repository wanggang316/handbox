---
description: Create a git commit
---

Create a git commit with the staged changes.

Analyze the changes with `git diff --cached` and `git status`, then create a commit message following Conventional Commits format (e.g., `feat(scope): message`, `fix: message`).

Common types: feat, fix, refactor, docs, test, chore
Common scopes: backend, frontend, database, api
